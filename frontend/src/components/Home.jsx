import styled from 'styled-components'

const Home = (props) => {
    return (
        <div>
            <Title>Home</Title>
        </div>
    )
}

const Title = styled.h1`
    font-family: 'Indie Flower', cursive;
    font-weight: 900;
    font-size: 300%;
`

export default Home