import NavBar from './Navbar'

const Dashboard = (props) => {
    return (
        <div>
            <NavBar path_to="/dashboard"/>
            <h1>Dashboard</h1>
        </div>
    )
}

export default Dashboard